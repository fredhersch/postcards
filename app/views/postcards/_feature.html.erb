<div id="section-<%= feature_counter + 1%>">
	<div class="image-wrapper">
		<div class="text-over-image">
			<h2><%= link_to feature.title, feature.title %></h2>
			<p><%= feature.description %></p>
		</div>
		<%= image_tag feature.photo.url(:original) %>
	</div>
</div>
